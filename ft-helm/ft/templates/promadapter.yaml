apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-adapter-client
data:
  config.yaml: |
    rules:
    - seriesQuery: 'ft_queue_zip_size'
      resources:
        overrides:
          namespace: {resource: "namespace"}
          pod: {resource: "pod"}
      name:
        matches: "^ft_queue_zip_size$"
        as: "scale_ft_queue_zip_size"
      metricsQuery: 'sum(clamp_min(predict_linear(ft_queue_zip_size[2m], 120), 0)) by (<<.GroupBy>>)'